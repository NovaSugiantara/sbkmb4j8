{
  "name": "candle-collection-log",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "npm run build && node --test \"tests/*.test.mjs\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
